name: CI/CD Docker
on:
  push:
    branches:
      - master
    tags:
      - v[0-9]+.[0-9]+**
  pull_request:
    branches:
      - master

jobs:
  build:
    uses: ./.github/workflows/build.yml@main

  deploy:
    needs: build
    uses: ./.github/workflows/deploy.yml@main