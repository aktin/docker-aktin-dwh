name: Test Container Update Check

on:
  workflow_dispatch:

jobs:
  test-postgresql:
    uses: ./.github/workflows/reusable/check-container-update.yml
    with:
      container: postgres
      version_key: POSTGRESQL_VERSION
